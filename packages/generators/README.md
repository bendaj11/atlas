# @atlas/generators

Project templates used by `@atlas/cli` to generate React and Angular Atlas hosts and microfrontends.

Most applications should install `@atlas/cli` rather than invoke this package directly.
