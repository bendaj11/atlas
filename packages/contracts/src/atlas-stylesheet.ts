/** CSS file produced by a build that the host should load before showing the app. */
export interface AtlasStylesheet {
  /** Full URL to the CSS file. */
  href: string;
  /** Optional browser integrity check so Atlas can reject changed or corrupted CSS. */
  integrity?: string;
}
