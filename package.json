{
  "name": "atlas-platform",
  "version": "0.4.6",
  "private": true,
  "description": "TypeScript-first app platform with focused SDK, runtime, static registry, generators and test tooling.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/bendaj11/atlas.git"
  },
  "homepage": "https://github.com/bendaj11/atlas#readme",
  "bugs": "https://github.com/bendaj11/atlas/issues",
  "type": "module",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "turbo run build --filter=\"@atlas/*\"",
    "build:examples": "turbo run build --concurrency=1 --filter=\"@atlas-example/*\" --filter=\"@atlas-app/*\"",
    "ci:affected": "pnpm run typecheck:affected && pnpm run test:affected",
    "ci:full": "pnpm run typecheck && pnpm run test",
    "pack:verify": "tsx scripts/verify-packages.ts",
    "release": "tsx scripts/release.ts",
    "release:publish": "tsx scripts/publish-release.ts",
    "test:generated": "pnpm run pack:verify && tsx scripts/verify-generated-projects.ts --package-manager=yarn && tsx scripts/verify-generated-projects.ts --package-manager=pnpm",
    "test": "turbo run test --concurrency=1 --filter=\"@atlas/*\" && node --experimental-vm-modules node_modules/jest/bin/jest.js --config jest.config.json --roots scripts",
    "test:affected": "turbo run test --concurrency=1 --affected --filter=\"@atlas/*\"",
    "test:e2e": "tsx tests/e2e/prepare.ts && playwright test",
    "test:container": "pnpm run pack:verify && tsx scripts/verify-bootstrap-container.ts",
    "test:e2e:prepare": "tsx tests/e2e/prepare.ts",
    "typecheck": "turbo run typecheck --filter=\"@atlas/*\" && tsc -p tsconfig.tests.json && tsc -p tsconfig.tooling.json",
    "typecheck:affected": "turbo run typecheck --affected --filter=\"@atlas/*\"",
    "atlas": "node packages/cli/dist/cli/entrypoint.js"
  },
  "devDependencies": {
    "@jest/globals": "29.7.0",
    "@playwright/test": "^1.61.1",
    "@types/jest": "29.5.14",
    "@types/node": "^22",
    "cross-spawn": "7.0.6",
    "jest": "29.7.0",
    "prettier": "3.9.5",
    "tar": "7.5.21",
    "ts-jest": "29.2.5",
    "tsx": "^4.23.7",
    "turbo": "^2.6.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=22.12.0 <23",
    "pnpm": ">=10 <11"
  }
}
